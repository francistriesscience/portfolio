{
  "name": "@portfolio/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "generate:notes": "bun run ./scripts/generate-notes.ts",
    "dev": "bun run generate:notes && next dev --port 3000",
    "build": "bun run generate:notes && next build",
    "start": "next start",
    "lint": "next lint",
    "build:cloudflare": "bun run generate:notes && opennextjs-cloudflare build",
    "preview:cloudflare": "bun run generate:notes && opennextjs-cloudflare build && opennextjs-cloudflare preview",
    "upload:cloudflare": "bun run generate:notes && opennextjs-cloudflare build && opennextjs-cloudflare upload",
    "deploy:cloudflare": "bun run generate:notes && opennextjs-cloudflare build && opennextjs-cloudflare deploy",
    "typegen:cloudflare": "wrangler types --env-interface CloudflareEnv ./cloudflare-env.d.ts"
  },
  "dependencies": {
    "@opennextjs/cloudflare": "^1.17.1",
    "@packages/metadata": "*",
    "@packages/ui": "*",
    "gray-matter": "^4.0.3",
    "katex": "^0.16.22",
    "next": "16.1.6",
    "next-mdx-remote": "^6.0.0",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "rehype-katex": "^7.0.1",
    "remark-gfm": "^4.0.1",
    "remark-math": "^6.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@eslint/eslintrc": "^3",
    "@tailwindcss/postcss": "^4",
    "@types/node": "^25.5.0",
    "@types/react": "^19.1.5",
    "@types/react-dom": "^19.1.5",
    "eslint": "^9",
    "eslint-config-next": "15.4.6",
    "tailwindcss": "^4",
    "typescript": "^5.7.4",
    "wrangler": "^4.74.0"
  },
  "ignoreScripts": [
    "sharp",
    "unrs-resolver"
  ],
  "trustedDependencies": [
    "sharp",
    "unrs-resolver"
  ]
}
